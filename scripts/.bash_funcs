## Functions file
## If you do not already have one I recommend placing it in ~/
## and then calling from .bashrc / .profile with:
##
## if [ -f ~/.bash_funcs ] ; then
##   . ~/.bash_funcs
## fi

alias BAT_CHARGE="whatever dir you cloned this to scripts/batterylevel.py"
function battery_charge {
    echo `$BAT_CHARGE` 2>/dev/null
}
